{
  "name": "anestbot2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "ANESTBOT 2.0 — triagem pré-anestésica no WhatsApp (UltraMsg + Claude), arquitetura webhook-first",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "STATE_DIR=/tmp/anestbot2-test node test/run.mjs"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
